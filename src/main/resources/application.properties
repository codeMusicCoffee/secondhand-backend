# ===============================
# 应用基础配置
# ===============================
spring.application.name=secondhand
server.port=8080


# ===============================
# ? MySQL ?????
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/demo?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=MySql@123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# ? JPA / Hibernate ??
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ? ?? MySQL 8 ????????
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ===============================
# ? ??????????????????
# ===============================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ??????????
app.upload.dir=uploads
# app.upload.path=${user.dir}/backend/secondhand/uploads

# ===============================
# CORS ??
# ===============================
# ??CORS
spring.web.cors.allowed-origins=http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173,http://127.0.0.1:5174
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# ===============================
# JWT ??
# ===============================
# JWT????? (??32???)
jwt.secret=mySecretKey123456789012345678901234567890
# JWT?????? (???) - 24??
jwt.expiration=86400000
# JWT?????
jwt.header=Authorization
# JWT token??
jwt.token-prefix=Bearer 
# ===============================
# Redis 配置
# ===============================
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.database=0
spring.data.redis.timeout=2000ms
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0
spring.data.redis.lettuce.pool.max-wait=-1ms